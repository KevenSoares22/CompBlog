import React from 'react';
const Home = () => {
  const pageTitle = 'Olá, seja bem vindo';

  return (
    <div>
      <h1 className="introdution">{pageTitle}</h1>
    </div>
  );
};

export default Home;
